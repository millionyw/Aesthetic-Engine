# .ai_context

## 项目摘要
构建一个本地运行的基于 CLIP 的个人审美偏好引擎，用少量标注实现大规模图片筛选。

## 技术栈约定
- Python 3.10+ (小于 3.13)
- PyTorch（必须适配 MPS/Mac 或 CUDA/Nvidia 加速）
- HuggingFace Transformers（CLIP 模型）
- facenet-pytorch
- ultralytics
- sqlite3（内置）
- streamlit-shortcuts
- 数据存储格式：Pickle/JSON
- 存储规范：所有模型（HuggingFace, PyTorch Hub, YOLO）必须强制下载并缓存在 ./data/models 目录下
- Linter: ruff check .
- Type Checker: 不使用 mypy，保留一定的灵活性

## 架构决策
选择 Linear Probing 而非 Fine-tuning：冻结大模型骨干，仅训练末端小模型，以降低样本需求、算力成本并提升隐私场景的可控性。
使用 Global CLIP + YOLO Person Crop CLIP + FaceNet 构建 2048 维混合特征，用于统一评分与可解释性分析。

## 项目详细架构
- 核心目录结构
  - /data/raw_images/（原图）
  - /data/models/（模型缓存）
  - /data/gallery.db（SQLite）
  - /src/（核心源码）
- 核心模块职责
  - ingest_images.py（ETL/入库）
  - feature_extractor.py（特征推理）
  - db.py（持久化）
  - app.py（UI/交互）
- 数据库表结构
  - images: path (PK), pred_score, human_score, timestamp

## 底层实现规范
- 特征向量结构（2048-dim）
  - [0:768] Global Feature (CLIP-ViT 全局语义)。
  - [768:1536] Body Feature (YOLO 人体裁剪后经过 CLIP 提取)。
  - [1536:2048] Face Feature (FaceNet 提取的五官及面部比例特征)。
  - 权重解析原则：贡献度计算必须按维度数（768, 768, 512）进行均值归一化，以消除维度基数不均带来的偏差。
- UI 状态管理原则 (State Management)
  - **st.session_state**: 
    - 必须使用 `dialog_idx` 管理预览弹窗中的当前图片索引。
    - 必须使用 `st.cache_data` 处理缩略图生成以保证滚动性能。
    - 标注大厅与 Dialog 打分必须使用 `streamlit-shortcuts` 绑定 `1-5` 键位。
  - **状态流转**: 
    - 评分反馈触发顺序：更新数据库 -> 追加 labels.csv -> 触发静默重训 -> `st.rerun()`。
    - 弹窗关闭必须手动清理索引状态，防止下次打开时位置错乱。

## 数据流程 (Data Workflow)
1. **入库**: 扫描本地 -> 提取三合一特征 -> Ridge 预测 -> 存入 SQLite。
2. **预览**: 按 `COALESCE(human, pred)` 降序读取 DB -> 渲染 4 列缩略图网格。
3. **闭环**: 用户在 Dialog 中使用键盘 (A/D/←/→) 翻页并打分 -> 增量训练模型 -> 实时更新预测分。

## 当前进度
- **Phase 1: Project Setup**: 环境搭建，确立基于 CLIP 的 Linear Probing 架构决策。 (Completed)
- **Phase 2: Global Feature Extraction**: 实现基础 CLIP 全局特征提取与持久化。 (Completed)
- **Phase 3: Regression Training**: 引入 Ridge 回归模型，实现初步审美打分预测。 (Completed)
- **Phase 4: Active Learning**: 增加主动学习模式，优化标注样本推荐策略。 (Completed)
- **Phase 5: UI Prototyping**: 使用 Streamlit 搭建基础打分界面。 (Completed)
- **Phase 6: Hybrid Features**: 集成 YOLO 人体裁剪与 FaceNet，升级为 2048 维混合特征。 (Completed)
- **Phase 7: Performance Profiling**: 全链路耗时统计与 labels.csv 预测对比优化。 (Completed)
- **Phase 8: Full-Stack Integration**: 引入 SQLite 存储，实现带解释性输出的审美图库与反馈重训闭环。 (Completed)
- **Phase 8.5: UI Polish & Refactoring**: 优化图库浏览体验，重构扫描脚本为 ingest_images.py。 (Completed)
- **Phase 9: Performance & Immersive Gallery**: 高性能缩略图网格、st.dialog 沉浸式预览、键盘监听。 (Completed)

## Memory Bank(经验沉淀)
CLIP 模型加载应优先使用本地缓存路径；特征提取时需注意 Projection 层的维度匹配。
对于小样本审美预测，Ridge 回归在 CLIP 特征上表现稳健。
采用 RidgeCV 自动搜索正则化强度，并引入 StandardScaler 解决多模态特征尺度不一和分数压缩问题。
Streamlit 性能优化：对于大图列表，必须使用内存缩略图字节流加载。
可解释性修正：多模态特征贡献度需消除维度基数偏差。
